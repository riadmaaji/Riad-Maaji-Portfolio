---
interface Props {
  name: string;
  icon?: string;
}

const { name } = Astro.props;
---

<span class="inline-flex items-center gap-2 px-4 py-2 bg-zinc-800/80 text-zinc-200 rounded-lg text-sm font-medium border border-zinc-700/50 skill-glow cursor-default">
  {name}
</span>
